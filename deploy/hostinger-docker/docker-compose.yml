services:
  bot:
    build:
      context: .
      dockerfile: deploy/hostinger-docker/Dockerfile
    restart: unless-stopped
    environment:
      POLY_PRIVATE_KEY: ${POLY_PRIVATE_KEY}
      POLY_FUNDER: ${POLY_FUNDER}
      POLY_SIGNATURE_TYPE: ${POLY_SIGNATURE_TYPE:-1}
      POLY_DRY_RUN: ${POLY_DRY_RUN:-true}
      RELAYER_API_KEY: ${RELAYER_API_KEY:-}
      RELAYER_SECRET: ${RELAYER_SECRET:-}
      RELAYER_PASSPHRASE: ${RELAYER_PASSPHRASE:-}
      BOT_LOG_LEVEL: ${BOT_LOG_LEVEL:-INFO}
      BOT_POLL_INTERVAL_SECONDS: ${BOT_POLL_INTERVAL_SECONDS:-1.0}
      BOT_REQUEST_TIMEOUT_SECONDS: ${BOT_REQUEST_TIMEOUT_SECONDS:-10.0}
      BOT_STRATEGY_MODE: ${BOT_STRATEGY_MODE:-paladin}
      BOT_PALADIN_MAX_SHARES_PER_SIDE: ${BOT_PALADIN_MAX_SHARES_PER_SIDE:-10}
      BOT_PALADIN_MIN_ELAPSED_BETWEEN_PAIR_STARTS: ${BOT_PALADIN_MIN_ELAPSED_BETWEEN_PAIR_STARTS:-100}
      BOT_PALADIN_ENTRY_TRAILING_MIN_LOW_SEC: ${BOT_PALADIN_ENTRY_TRAILING_MIN_LOW_SEC:--1}
      BOT_PALADIN_TARGET_MIN_ROI: ${BOT_PALADIN_TARGET_MIN_ROI:-0}
      BOT_PALADIN_STAGGER_ALTERNATE_FIRST_WHEN_BALANCED: ${BOT_PALADIN_STAGGER_ALTERNATE_FIRST_WHEN_BALANCED:-true}
      BOT_PALADIN_STAGGER_HEDGE_FORCE_SEC: ${BOT_PALADIN_STAGGER_HEDGE_FORCE_SEC:-90}
      BOT_PALADIN_MAX_BLENDED_PAIR_AVG_SUM: ${BOT_PALADIN_MAX_BLENDED_PAIR_AVG_SUM:-0.97}
      BOT_PALADIN_STAGGER_SYMMETRIC_FALLBACK_SKIP_FIRST_BLEND: ${BOT_PALADIN_STAGGER_SYMMETRIC_FALLBACK_SKIP_FIRST_BLEND:-false}
      BOT_SHARES_PER_LEVEL: ${BOT_SHARES_PER_LEVEL:-6}
      BOT_STRATEGY_BUDGET_CAP_USDC: ${BOT_STRATEGY_BUDGET_CAP_USDC:-80.0}
      BOT_STRATEGY_WALLET_RESERVE_USDC: ${BOT_STRATEGY_WALLET_RESERVE_USDC:-0.0}
      BOT_STRATEGY_MIN_BUDGET_USDC: ${BOT_STRATEGY_MIN_BUDGET_USDC:-15.0}
      BOT_STRATEGY_ENTRY_DELAY_SECONDS: ${BOT_STRATEGY_ENTRY_DELAY_SECONDS:-24}
      BOT_STRATEGY_NEW_ORDER_CUTOFF_SECONDS: ${BOT_STRATEGY_NEW_ORDER_CUTOFF_SECONDS:-30}
      BOT_FORCE_EXIT_BEFORE_END_SECONDS: ${BOT_FORCE_EXIT_BEFORE_END_SECONDS:-15}
